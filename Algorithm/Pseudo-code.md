area = L*B
#Algorithm for area_of_square
Step1: Start
Step2: Read L,B
Step3: Area := L * B
Step4: Print(Area)
Step5: End


#Algorithm ATM_Machine
Step1: Start
Step2: Read acct_no, pin,amt_inputted,acct_balance
Step3: 
          if(amt_inputted > acct_balance)
          Print("insufficient fund")
         else
          Print("Amount successfully withdrawn")
Step4: End          
 
Write the pseudo code to determine if a number inputted is an even or odd number

.algo(odd,even numbers)
   ALGORITHM numbers
   VAR 
      odd numbers: FLOAT
      even numbers: INTEGER;
   BEGIN
    If number MOD 2=0 THEN
     OUTPUT "the output is even"
   ELSE
      OUTPUT "the output is odd"
   END IF     